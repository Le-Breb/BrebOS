#ifndef INCLUDE_CTYPE_H
#define INCLUDE_CTYPE_H

#include <kstddef.h>

bool isdigit(uint c);

bool isupper(char c);

bool islower(char c);

bool isalpha(char c);

bool isalnum(char c);

#endif //INCLUDE_CTYPE_H
